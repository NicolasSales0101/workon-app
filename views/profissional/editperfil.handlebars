{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

    <style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }

      .mb-n0.5 {
            margin-bottom: -0.5rem !important;
        }

        .mb-n1 {
            margin-bottom: -1rem !important;
        }

    </style>


  </head>
 
<div class="row justify-content-center">
<main class="form-signin text-center shadow-lg p-3 mb-4 bg-body rounded-3" style="width: 90%;">
  <form action="/profissional/perfil/edit" method="post" enctype="multipart/form-data">
  <input type="hidden" name="id" value="{{usuarios.dataValues.id}}">
  <!--<img class="mb-n0.5" src="/img/{{usuarios.dataValues.profile_img}}" alt="" width="150" height="150">-->
      <div class="avatar-upload">
        <div class="avatar-edit">
            <input type='file' id="imageUpload" accept=".png, .jpg, .jpeg" name="file">
            <label for="imageUpload"></label>
        </div>
        <div class="avatar-preview">
            <div id="imagePreview" style="background-image: url(/uploads/{{usuarios.dataValues.profile_img}});">
            </div>
        </div>
    </div>
    <h1 class="h3 mb-3 fw-normal">Edite seu perfil</h1>

    <div class="row g-2">
        <div class="col-md">
            <div class="form-floating mb-2">
                <input type="text" name="nome" class="form-control" value="{{usuarios.dataValues.nome}}"  id="floatingInput" placeholder="nome" required>
                <label for="floatingInput">Nome</label>
            </div>
        </div>

        <div class="col-md">
           <div class="form-floating mb-2">
                <input type="text" name="cidade" class="form-control" id="floatingInput" value="{{usuarios.dataValues.cidade}}" placeholder="Cidade">
                <label for="floatingInput">Cidade</label>
           </div>
        </div>

        <div class="col-md">
            <div class="form-floating mb-2">
                <input type="text" name="telefone" class="form-control" id="floatingInput" value="{{usuarios.dataValues.telefone}}" placeholder="99999-9999">
                <label for="floatingInput">Telefone</label>
            </div>
        </div>
    </div>

    <div class="form-floating mb-2">
      <textarea name="descricao" class="form-control mb-2 ps-3" style="min-height: 150px; max-height: 300px;" id="floatingTextarea" spellcheck="true" placeholder="Descrição">{{usuarios.dataValues.descricao}}</textarea>
      <label for="floatingTextarea" class="ps-3">Descrição</label>
    </div>

    <div class="form-floating mb-2">
      <select name="profissao" class="form-select" id="floatingSelect">
                {{#each profissionais}}
                    <option value="{{dataValues.id}}">{{dataValues.profissao}}</option>
                {{else}}
                    <option value="0">Nenhuma profissao registrada</option>
                {{/each}}
            </select>
      <label for="floatingSelect">Profissão</label>
    </div>
    <!--
    <input type="file" name="file" class="mb-2"><br>
    -->
    <button class="w-100 btn btn-lg btn-primary" type="submit">Editar Perfil</button>
    <p class="mt-5 mb-3 text-muted">&copy; 2017–2021</p>
  </form>
</main>
<hr class="featurette-divider invisible">
    <div class="text-center">
        <p class="lead text-muted fs-3"><b>Edite seu perfil a votande!</b> Coloque emojis 🐳 usando o atalho do Windows 10 <kbd class="bg-secondary">Windows</kbd> + <kbd class="bg-secondary">.</kbd>, coloque comentários sobre você entre outras coisas que seja interessante para os clientes! </p>
    </div>
</div>

<script src='https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js'></script><script  src="/js/script.js"></script>